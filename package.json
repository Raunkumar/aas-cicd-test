{
  "name": "aas-cicd-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "scripts": {
  "build": "vite build",
  "test": "jest --coverage",
  "lint": "eslint . --ext .js,.jsx"
},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0",
    "jest": "^30.2.0",
    "supertest": "^7.1.4"
  }
}

"scripts": {
  "build": "vite build",
  "test": "jest --coverage",
  "lint": "eslint . --ext .js,.jsx"
}
